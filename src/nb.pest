WHITESPACE = _{ " " }

value = @{ "true" | "false" }

tvar = @{ "t" ~ ASCII_DIGIT+ }

assignment = { tvar ~ "=" ~ value }

conditional = { "if" ~ tvar ~ "then" ~ tvar ~  "else" ~  tvar }

term = { value |  conditional }

statement = { term | assignment}