WHITESPACE = _{ " " }
value = @{ "true" | "false" }
tvar = @{ "t" ~ ASCII_DIGIT+ }
assignment = { tvar ~ "=" ~ value }
term = { value | "if" ~ tvar ~ "then" ~ tvar ~  "else" ~  tvar }
statement = { term | assignment}