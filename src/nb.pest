WHITESPACE = _{ " " }

value = { "true" | "false" }

conditional = { "if" ~ term ~ "then" ~ term ~  "else" ~ term  }

tvar = @{ "t" ~ ASCII_DIGIT+ }

assignment = { tvar ~ "=" ~ value }

term = { value | tvar | conditional }

statement = { assignment | term | tvar }